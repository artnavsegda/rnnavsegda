import {StyleSheet} from 'react-native';
import theme from '../../../doing/theme';

//@flow

export default StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        //   backgroundColor: '#edeef2',
    },
    emptyInfoBlock: {
        alignItems: 'center',
        paddingHorizontal: 20,
        justifyContent: 'center',
        ///   backgroundColor: '#edeef2',
    },
});
